import {StringProperty} from "../StringProperty"


type Properties = {
  IdentityPoolId: StringProperty
  Roles?: any
  Id?: StringProperty
  RoleMappings?: any
}

export const AWSCognitoIdentityPoolRoleAttachment = ({
                                                       ResourceName,
                                                       DependsOn,
                                                       Properties,
                                                     }: {
  ResourceName: string
  DependsOn?: string | string[]
  Properties: Record<string, any> & Properties
}) => ({
  Resources: {
    [ResourceName]: {
      Type: 'AWS::Cognito::IdentityPoolRoleAttachment',
      DependsOn,
      Properties,
    }
  },
  Outputs: {
    [ResourceName]: {
      Value: {
        "Ref": ResourceName,
      },
      Export: {
        Name: {
          "Fn::Sub": "stack:${AWS::StackName}:" + ResourceName
        }
      }
    }
  }
})