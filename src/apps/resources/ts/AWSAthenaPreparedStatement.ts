import {StringProperty} from "../StringProperty"


type Properties = {
  StatementName: StringProperty
  WorkGroup: StringProperty
  Description?: StringProperty
  QueryStatement: StringProperty
}

export const AWSAthenaPreparedStatement = ({
                                             ResourceName,
                                             DependsOn,
                                             Properties,
                                           }: {
  ResourceName: string
  DependsOn?: string | string[]
  Properties: Record<string, any> & Properties
}) => ({
  Resources: {
    [ResourceName]: {
      Type: 'AWS::Athena::PreparedStatement',
      DependsOn,
      Properties,
    }
  },
  Outputs: {
    [ResourceName]: {
      Value: {
        "Ref": ResourceName,
      },
      Export: {
        Name: {
          "Fn::Sub": "stack:${AWS::StackName}:" + ResourceName
        }
      }
    }
  }
})