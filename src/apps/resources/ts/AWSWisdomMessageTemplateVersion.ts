import {StringProperty} from "../StringProperty"


type Properties = {
  MessageTemplateArn: StringProperty
  MessageTemplateVersionArn?: StringProperty
  MessageTemplateContentSha256?: StringProperty
  MessageTemplateVersionNumber?: number
}

export const AWSWisdomMessageTemplateVersion = ({
                                                  ResourceName,
                                                  DependsOn,
                                                  Properties,
                                                }: {
  ResourceName: string
  DependsOn?: string | string[]
  Properties: Record<string, any> & Properties
}) => ({
  Resources: {
    [ResourceName]: {
      Type: 'AWS::Wisdom::MessageTemplateVersion',
      DependsOn,
      Properties,
    }
  },
  Outputs: {
    [ResourceName]: {
      Value: {
        "Ref": ResourceName,
      },
      Export: {
        Name: {
          "Fn::Sub": "stack:${AWS::StackName}:" + ResourceName
        }
      }
    }
  }
})