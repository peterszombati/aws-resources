import {StringProperty} from "../StringProperty"


type Properties = {
  RegexString?: StringProperty
  ContextWords?: StringProperty[]
  Id?: StringProperty
  Tags?: Record<string, any>
  Name?: StringProperty
}

export const AWSGlueCustomEntityType = ({
                                          ResourceName,
                                          DependsOn,
                                          Properties,
                                        }: {
  ResourceName: string
  DependsOn?: string | string[]
  Properties: Record<string, any> & Properties
}) => ({
  Resources: {
    [ResourceName]: {
      Type: 'AWS::Glue::CustomEntityType',
      DependsOn,
      Properties,
    }
  },
  Outputs: {
    [ResourceName]: {
      Value: {
        "Ref": ResourceName,
      },
      Export: {
        Name: {
          "Fn::Sub": "stack:${AWS::StackName}:" + ResourceName
        }
      }
    }
  }
})