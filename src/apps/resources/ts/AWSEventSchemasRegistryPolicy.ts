import {StringProperty} from "../StringProperty"


type Properties = {
  Id?: StringProperty
  Policy: Record<string, any>
  RegistryName: StringProperty
  RevisionId?: StringProperty
}

export const AWSEventSchemasRegistryPolicy = ({
                                                ResourceName,
                                                DependsOn,
                                                Properties,
                                              }: {
  ResourceName: string
  DependsOn?: string | string[]
  Properties: Record<string, any> & Properties
}) => ({
  Resources: {
    [ResourceName]: {
      Type: 'AWS::EventSchemas::RegistryPolicy',
      DependsOn,
      Properties,
    }
  },
  Outputs: {
    [ResourceName]: {
      Value: {
        "Ref": ResourceName,
      },
      Export: {
        Name: {
          "Fn::Sub": "stack:${AWS::StackName}:" + ResourceName
        }
      }
    }
  }
})