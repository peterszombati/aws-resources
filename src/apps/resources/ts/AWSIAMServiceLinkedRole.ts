import {StringProperty} from "../StringProperty"


type Properties = {
  RoleName?: StringProperty
  CustomSuffix?: StringProperty
  Description?: StringProperty
  AWSServiceName?: StringProperty
}

export const AWSIAMServiceLinkedRole = ({
                                          ResourceName,
                                          DependsOn,
                                          Properties,
                                        }: {
  ResourceName: string
  DependsOn?: string | string[]
  Properties: Record<string, any> & Properties
}) => ({
  Resources: {
    [ResourceName]: {
      Type: 'AWS::IAM::ServiceLinkedRole',
      DependsOn,
      Properties,
    }
  },
  Outputs: {
    [ResourceName]: {
      Value: {
        "Ref": ResourceName,
      },
      Export: {
        Name: {
          "Fn::Sub": "stack:${AWS::StackName}:" + ResourceName
        }
      }
    }
  }
})