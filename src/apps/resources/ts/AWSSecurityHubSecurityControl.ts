import {StringProperty} from "../StringProperty"


type Properties = {
  SecurityControlId?: StringProperty
  SecurityControlArn?: StringProperty
  LastUpdateReason?: StringProperty
  Parameters: Record<string, any>
}

export const AWSSecurityHubSecurityControl = ({
                                                ResourceName,
                                                DependsOn,
                                                Properties,
                                              }: {
  ResourceName: string
  DependsOn?: string | string[]
  Properties: Record<string, any> & Properties
}) => ({
  Resources: {
    [ResourceName]: {
      Type: 'AWS::SecurityHub::SecurityControl',
      DependsOn,
      Properties,
    }
  },
  Outputs: {
    [ResourceName]: {
      Value: {
        "Ref": ResourceName,
      },
      Export: {
        Name: {
          "Fn::Sub": "stack:${AWS::StackName}:" + ResourceName
        }
      }
    }
  }
})