import {StringProperty} from "../StringProperty"


type Properties = {
  KeyId: StringProperty
  KeyType: (string | "API_KEY")
  UsagePlanId: StringProperty
  Id?: StringProperty
}

export const AWSApiGatewayUsagePlanKey = ({
                                            ResourceName,
                                            DependsOn,
                                            Properties,
                                          }: {
  ResourceName: string
  DependsOn?: string | string[]
  Properties: Record<string, any> & Properties
}) => ({
  Resources: {
    [ResourceName]: {
      Type: 'AWS::ApiGateway::UsagePlanKey',
      DependsOn,
      Properties,
    }
  },
  Outputs: {
    [ResourceName]: {
      Value: {
        "Ref": ResourceName,
      },
      Export: {
        Name: {
          "Fn::Sub": "stack:${AWS::StackName}:" + ResourceName
        }
      }
    }
  }
})