import {StringProperty} from "../StringProperty"


type Properties = {
  Accounts?: StringProperty[]
  DataLakeArn: StringProperty
  SourceName: StringProperty
  SourceVersion: StringProperty
}

export const AWSSecurityLakeAwsLogSource = ({
                                              ResourceName,
                                              DependsOn,
                                              Properties,
                                            }: {
  ResourceName: string
  DependsOn?: string | string[]
  Properties: Record<string, any> & Properties
}) => ({
  Resources: {
    [ResourceName]: {
      Type: 'AWS::SecurityLake::AwsLogSource',
      DependsOn,
      Properties,
    }
  },
  Outputs: {
    [ResourceName]: {
      Value: {
        "Ref": ResourceName,
      },
      Export: {
        Name: {
          "Fn::Sub": "stack:${AWS::StackName}:" + ResourceName
        }
      }
    }
  }
})