import {StringProperty} from "../StringProperty"


type Properties = {
  Id?: StringProperty
  Broker: StringProperty
  Configuration: {
    Revision: number
    Id: StringProperty
  }
}

export const AWSAmazonMQConfigurationAssociation = ({
                                                      ResourceName,
                                                      DependsOn,
                                                      Properties,
                                                    }: {
  ResourceName: string
  DependsOn?: string | string[]
  Properties: Record<string, any> & Properties
}) => ({
  Resources: {
    [ResourceName]: {
      Type: 'AWS::AmazonMQ::ConfigurationAssociation',
      DependsOn,
      Properties,
    }
  },
  Outputs: {
    [ResourceName]: {
      Value: {
        "Ref": ResourceName,
      },
      Export: {
        Name: {
          "Fn::Sub": "stack:${AWS::StackName}:" + ResourceName
        }
      }
    }
  }
})