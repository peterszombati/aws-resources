import {StringProperty} from "../StringProperty"


type Properties = {
  VersionId?: StringProperty
  TypeName?: StringProperty
  Arn?: StringProperty
  TypeVersionArn?: StringProperty
}

export const AWSCloudFormationResourceDefaultVersion = ({
                                                          ResourceName,
                                                          DependsOn,
                                                          Properties,
                                                        }: {
  ResourceName: string
  DependsOn?: string | string[]
  Properties: Record<string, any> & Properties
}) => ({
  Resources: {
    [ResourceName]: {
      Type: 'AWS::CloudFormation::ResourceDefaultVersion',
      DependsOn,
      Properties,
    }
  },
  Outputs: {
    [ResourceName]: {
      Value: {
        "Ref": ResourceName,
      },
      Export: {
        Name: {
          "Fn::Sub": "stack:${AWS::StackName}:" + ResourceName
        }
      }
    }
  }
})