import {StringProperty} from "../StringProperty"


type Properties = {
  ProfileName: StringProperty
  ProfileVersion?: StringProperty
  Action: StringProperty
  Principal: StringProperty
  StatementId: StringProperty
}

export const AWSSignerProfilePermission = ({
                                             ResourceName,
                                             DependsOn,
                                             Properties,
                                           }: {
  ResourceName: string
  DependsOn?: string | string[]
  Properties: Record<string, any> & Properties
}) => ({
  Resources: {
    [ResourceName]: {
      Type: 'AWS::Signer::ProfilePermission',
      DependsOn,
      Properties,
    }
  },
  Outputs: {
    [ResourceName]: {
      Value: {
        "Ref": ResourceName,
      },
      Export: {
        Name: {
          "Fn::Sub": "stack:${AWS::StackName}:" + ResourceName
        }
      }
    }
  }
})