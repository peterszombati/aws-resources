import {StringProperty} from "../StringProperty"


type Properties = {
  ExperimentTemplateId: StringProperty
  AccountId: StringProperty
  RoleArn: StringProperty
  Description?: StringProperty
}

export const AWSFISTargetAccountConfiguration = ({
                                                   ResourceName,
                                                   DependsOn,
                                                   Properties,
                                                 }: {
  ResourceName: string
  DependsOn?: string | string[]
  Properties: Record<string, any> & Properties
}) => ({
  Resources: {
    [ResourceName]: {
      Type: 'AWS::FIS::TargetAccountConfiguration',
      DependsOn,
      Properties,
    }
  },
  Outputs: {
    [ResourceName]: {
      Value: {
        "Ref": ResourceName,
      },
      Export: {
        Name: {
          "Fn::Sub": "stack:${AWS::StackName}:" + ResourceName
        }
      }
    }
  }
})