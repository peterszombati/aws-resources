import {StringProperty} from "../StringProperty"


type Properties = {
  Id?: StringProperty
  Action: StringProperty
  LayerVersionArn: StringProperty
  OrganizationId?: StringProperty
  Principal: StringProperty
}

export const AWSLambdaLayerVersionPermission = ({
                                                  ResourceName,
                                                  DependsOn,
                                                  Properties,
                                                }: {
  ResourceName: string
  DependsOn?: string | string[]
  Properties: Record<string, any> & Properties
}) => ({
  Resources: {
    [ResourceName]: {
      Type: 'AWS::Lambda::LayerVersionPermission',
      DependsOn,
      Properties,
    }
  },
  Outputs: {
    [ResourceName]: {
      Value: {
        "Ref": ResourceName,
      },
      Export: {
        Name: {
          "Fn::Sub": "stack:${AWS::StackName}:" + ResourceName
        }
      }
    }
  }
})