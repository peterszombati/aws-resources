import {StringProperty} from "../StringProperty"


type Properties = {
  TypeVersionArn?: StringProperty
  TypeName?: StringProperty
  Arn?: StringProperty
  VersionId?: StringProperty
}

export const AWSCloudFormationHookDefaultVersion = ({
                                                      ResourceName,
                                                      DependsOn,
                                                      Properties,
                                                    }: {
  ResourceName: string
  DependsOn?: string | string[]
  Properties: Record<string, any> & Properties
}) => ({
  Resources: {
    [ResourceName]: {
      Type: 'AWS::CloudFormation::HookDefaultVersion',
      DependsOn,
      Properties,
    }
  },
  Outputs: {
    [ResourceName]: {
      Value: {
        "Ref": ResourceName,
      },
      Export: {
        Name: {
          "Fn::Sub": "stack:${AWS::StackName}:" + ResourceName
        }
      }
    }
  }
})